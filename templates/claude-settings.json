{
  "permissions": {
    "deny": [
      "Edit(chrome-styling.local.json)",
      "Write(chrome-styling.local.json)",
      "Edit(chrome-regions.local.json)",
      "Write(chrome-regions.local.json)",
      "Edit(chrome-contract.local.json)",
      "Write(chrome-contract.local.json)"
    ],
    "ask": [
      "Bash(*--accept*)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && [ -x node_modules/.bin/ucsd-decorator-kit ] || exit 0; node_modules/.bin/ucsd-decorator-kit verify || exit 2",
            "asyncRewake": true,
            "rewakeSummary": "Chrome integrity gate found a regression — do not run verify --accept yourself; surface this to the user and stop.",
            "timeout": 120
          },
          {
            "type": "command",
            "command": "[ -x \"${CLAUDE_PROJECT_DIR:-.}/node_modules/.bin/ucsd-decorator-kit\" ] || { echo 'The chrome integrity gate did not run: ucsd-decorator-kit is not installed in this project. Install it with: npm install --save-dev ucsd-decorator-kit' >&2; exit 1; }",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
